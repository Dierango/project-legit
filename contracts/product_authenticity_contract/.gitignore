# Genel Rust Derleme Çıktıları
# Cargo'nun derleme yaptığı varsayılan dizin
target/

# WASM çıktıları (yukarıdaki target/ kapsasa da, ek güvenlik için)
*.wasm

# Geliştirme Ortamı ve Konfigürasyon Dosyaları
# IDE veya editörlerin oluşturduğu geçici dosyalar
.vscode/
.idea/
*.iml
*.swp
*~

# Testler veya local CLI kullanımı sırasında oluşan geçici dosyalar
# Örneğin `soroban config identity generate` ile oluşan kimlik dosyaları
# Bunlar genellikle ~/.config/stellar/ altında olsa da, yanlışlıkla projeye düşebilirler.
.env
.DS_Store # macOS specific
.temp/
.local/
.gitkeep # Genellikle boş dizinleri git'e eklemek için kullanılır, pushlamayız
!/contracts/product_authenticity_contract/src/test.rs # Eğer test.rs dosyası varsa ve bunu git'e dahil etmek istiyorsan, bu satırı ekle.
# Eğer kontratlar içinde .vscode gibi klasörler olursa onları da ignore edebiliriz
# contracts/**/.vscode/
